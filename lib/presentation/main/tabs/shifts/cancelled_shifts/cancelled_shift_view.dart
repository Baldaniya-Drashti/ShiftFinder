import 'package:flutter/material.dart';

class CancelledShiftView extends StatelessWidget {
  const CancelledShiftView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Cancelled Shift'),
    );
  }
}
